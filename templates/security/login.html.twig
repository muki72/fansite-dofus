{% extends 'base.html.twig' %}

{% block title %}Connexion
{% endblock %}

{% block body %}

	<form method="post" class="form-section">


		{% if app.user %}
			<div class="mb-3">
				Vous êtes connecté en tant que
				{{ app.user.userIdentifier }},
				<a href="{{ path('app_logout') }}">Déconnexion</a>
			</div>
		{% endif %}

		<h1 class="form-title">Connexion</h1>
		<div class="form-container">

			<input type="email" value="{{ last_username }}" name="_username" id="username" class="form-input" autocomplete="email" placeholder="Email" required autofocus>


			<input type="password" name="_password" id="password" class="form-input" autocomplete="current-password" placeholder="Mot De Passe" required>


			<input type="hidden" name="_token" value="{{ csrf_token('authenticate') }}">
			{% if error %}
				<div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
			{% endif %}
			<div class="inline">
				<button class="btn-submit" type="submit">Connexion</button>
				<a href="{{ path('app_register') }}">S'enregistrer</a>
			</div>
		</div>
	</form>

{% endblock %}
