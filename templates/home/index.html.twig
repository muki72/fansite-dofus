{% extends 'base.html.twig' %}

{% block title %}Home
{% endblock %}

{% block body %}

	<div class="item-container">
		<div class="item-home-card">
			<h2>utilisateurs</h2>
			<div class="item-users-card">
				{% for user in users %}
					{% if user.img %}
						<a href="{{ path('app_user_show', {'id': user.id}) }}">
							<img class="home-icon" src="{{ asset('uploads/images/' ~ user.img) }}" alt="image de profile de {{user.userName}}"></a>
					{% else %}
						<a href="{{ path('app_user_show', {'id': user.id}) }}">
							<img class="home-icon" src="images/user_img.png" alt="image de profile de {{user.userName}}"></a>
					{% endif %}
				{% endfor %}

			</div>
		</div>
		<div class="item-home-card ">
			<a href="{{ path('app_portals') }}">
				<h2>Portails</h2>

				<img class="portal-background" src="{{ asset('/images/dim.jpg') }}" alt="portails">

			</a>
		</div>
		<div class="item-home-card">
			<a href="{{ path('app_almanax') }}">
				<h2 class="title">Almanax du jour</h2>
				<h3>{{ almanax.tribute.item.name }}
					x
					{{ almanax.tribute.quantity }}</h3>
				<img src="{{ almanax.tribute.item.image_urls.sd }}" alt="{{ almanax.tribute.item.name }}">
				<p>{{ almanax.bonus.description }}</p>
			</a>
		</div>
	</div>

	<div class="category-background">
		<h2 class="title" id="guides">guides</h2>
		<div class="categories-home">


			{% for category in categories %}

				<div class="categories-card">
					<h3 class="title">{{ category.title }}</h3>

					<a href="{{ path('app_category_show', {'id': category.id}) }}">
						<img src="{{ asset('uploads/images/' ~ category.img) }}" width="500" alt="image de la categorie {{category.title}}">
					</a>

				</div>
			{% endfor %}

		</div>
	</div>

	<div class="post-home">
		<div class="post-sort-background">
			<h2 class="title" id="postes">postes</h2>
			<h3>trier par :</h3>
			<div class="sort-btn">
				<a href="{{ path('home', {sort: 'date'}) }}#postes" class="btn-submit{% if currentSort == 'date' %} active{% endif %}">
					Date
					<i class="fa-solid fa-calendar"></i>
				</a>
				<a href="{{ path('home', {sort: 'votes'}) }}#postes" class="btn-submit{% if currentSort == 'votes' %} active{% endif %}">
					Vote
					<i class="fa-solid fa-thumbs-up"></i>
				</a>
			</div>
		</div>

	</div>

	<a class="btn-submit" href="{{ path('app_post_new') }}">Creer un Post</a>

	{% include 'post/index.html.twig' %}


{% endblock %}
