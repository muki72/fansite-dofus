<nav class="nav-bar">
    <a href="{{ path('home') }}"><img src="{{ asset('logo/logoplaceholder.png') }}" alt="Logo"></a>


    <div class="nav-index">
        <button class="btn-submit"><a href="{{ path('app_post_index') }}">Post</a></button>
        <button class="btn-submit"><a href="{{ path('app_reply_index') }}">Reply</a></button>
        <button class="btn-submit"><a href="{{ path('app_guide_index') }}">Guide</a></button>
        <button class="btn-submit"><a href="{{ path('app_category_index') }}">Category</a></button>
        {# {% if app.user %}
            <button class="btn-submit"><a href="{{ path('app_user_edit', { 'username': app.user.userName }) }}">Modifier mon profil</a></button>
        {% endif %} #}
    </div>
    
    <div class="nav-login">
        <button class="btn-submit">
            <a href="{{ path('app_login') }}">Se connecter</a>
        </button>
        <button class="btn-submit">
        <a href="{{ path('app_register') }}">S'enregistrer</a>
        </button>
    </div>
    
</nav>