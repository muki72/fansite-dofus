<nav class="nav-bar">
	<div class="nav-link">
		<a class="btn-submit" href="{{ path('home') }}#postes">Postes
			<i class="fa-solid fa-comments"></i>
		</a>
		<a class="btn-submit" href="{{ path('home') }}#guides">Guides
			<i class="fa-solid fa-book-bookmark"></i>
		</a>
		<a class="btn-submit" href="{{ path('home_rgpd') }}">Conditions
			<i class="fa-solid fa-scale-balanced"></i>
		</a>

	</div>

	<div class="logo-wrapper">
		<a href="{{ path('home') }}"><img src="{{ asset('images/logo_zaapy.png') }}" alt="Logo"></a>
	</div>

	<div class="nav-login">
		{% if is_granted('ROLE_ADMIN') %}

			<a class="btn-submit" href="{{ path('app_dashboard') }}">dashboard
				<i class="fa-solid fa-table"></i>
			</a>

		{% endif %}
		{% if app.user %}
			{% if app.user.img %}
				<a class="user-icon" href="{{ path('app_user_show', {'id': app.user.id}) }}">
					<img class="icon" src="{{ asset('uploads/images/' ~ app.user.img) }}" alt="image de profile de {{ app.user.userName }}">
				</a>
			{% else %}
				<a class="user-icon" href="{{ path('app_user_show', {'id': app.user.id}) }}">
					<img class="icon" src="..\images\user_img.png" alt="image de profile de {{ app.user.userName }}"></a>
			{% endif %}
		{% endif %}

		{% if not is_granted('ROLE_USER')%}

			<a class="btn-submit" href="{{ path('app_login') }}">Se connecter</a>

		{% endif %}
	</div>
</nav>
